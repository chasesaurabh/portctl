#!/usr/bin/env bash
set -euo pipefail

echo "Running integration tests..."
cd "$(dirname "$0")"

./test_kill.sh

echo "All integration tests passed."
#!/usr/bin/env bash
set -euo pipefail

echo "Running integration tests..."
cd "$(dirname "$0")"

./test_kill.sh

echo "All integration tests passed."
#!/usr/bin/env bash
set -euo pipefail

echo "Running integration tests..."
cd "$(dirname "$0")"

./test_kill.sh

echo "All integration tests passed."